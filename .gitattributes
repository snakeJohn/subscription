# 统一以 LF 存入仓库，检出时按平台转换
* text=auto eol=lf

# gradlew 必须是 LF，否则 Linux/macOS 上无法执行
gradlew text eol=lf
*.bat text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.ico binary
*.jar binary
*.apk binary
*.aab binary
